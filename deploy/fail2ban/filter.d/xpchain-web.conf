# fail2ban filter for xpchain-web
# Nginx access.log 에서 우리가 444 로 드롭한(=스캐너) 요청의 클라이언트 IP 를 잡는다.
# 기본 nginx combined 로그 포맷 기준:
#   $remote_addr - $remote_user [$time_local] "$request" $status ...
[Definition]
# "req" 를 [^"]* 로 경계지어 greedy 매칭이 상태코드를 놓치지 않게 한다.
# 우리 Nginx 스니펫이 스캐너를 444 로 드롭 → 그 IP 를 잡는다 (400/403 도 포함).
failregex = ^<HOST> -[^"]*"[^"]*"\s+(?:400|403|444)\s
ignoreregex =
