# fail2ban jail for xpchain-web
# 60초 안에 차단(444/400/403)을 4회 이상 유발한 IP 를 24시간 방화벽에서 밴.
[xpchain-web]
enabled  = true
filter   = xpchain-web
logpath  = /var/log/nginx/access.log
port     = http,https
maxretry = 4
findtime = 60
bantime  = 86400
action   = iptables-multiport[name=xpchain-web, port="http,https"]
